spring:
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/flowers
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver